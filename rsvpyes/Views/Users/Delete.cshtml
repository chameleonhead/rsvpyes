@model rsvpyes.Data.User

@{
    ViewData["Title"] = "Delete";
    var organization = ViewData["Organization"] as string;
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>User</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <form asp-action="Delete" asp-route-organization="@organization">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index" asp-route-organization="@ViewData["Organization"]">Back to List</a>
    </form>
</div>
